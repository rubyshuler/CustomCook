<%= form_with(model: recipe_ingredient, local: true) do |form| %>
  <% if recipe_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_ingredient.errors.count, "error") %> prohibited this recipe_ingredient from being saved:</h2>

      <ul>
      <% recipe_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.number_field :ingredient_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :measure %>
    <%= form.text_field :measure %>
  </div>

  <div class="field">
    <%= form.label :recipe_id %>
    <%#= form.number_field :recipe_id %>
    <%= select_tag("recipe_id", options_from_collection_for_select(@recipe, 'id', 'id')) %>
    </div>
    
    <!-- Artist.all.collect { |a| [a.name, a.id] } -->
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
