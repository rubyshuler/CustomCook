<%=
  react_component 'RecipeIngredient', {
    quantity: recipe_ingredient.quantity,
    measure: recipe_ingredient.measure,
    ingredient_id: Ingredient.find(recipe_ingredient.ingredient_id).name
  }
%>
