<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @step.description %>
</p>

<h2>Шаг <%= @step.position %></h2>

<div>
  <% if @step.step_image_url %>
    <%= image_tag(@step.step_image_url.to_s) %>
  <% end %>
</div>


<%= link_to 'Edit', edit_step_path(@step) %> |
<%= link_to 'Back', steps_path %>
